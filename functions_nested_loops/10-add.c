#include "main.h"
/**
 * add - add two integers and return the result
 * @a: integer value 1
 * @b: integer value 2
 * Return: the sum of the two integers
 **/
int add(int a, int b)
{
int  suma;
suma = a + b;
return (suma);
}
