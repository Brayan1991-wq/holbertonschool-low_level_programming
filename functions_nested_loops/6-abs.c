#include "main.h"
/**
 * _abs - absolute value of an integer
 *
 * @c:number to calculate
 *
 * Return:absolute value of zero
 **/
int _abs(int c)
{
if (c < 0)
{
int a;
a = c * -1;
return (a);
}
return (c);
}

