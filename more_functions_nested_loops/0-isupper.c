#include "main.h"
/**
 * _isupper - check the uppercase character
 * @c: character to check
 * Return: 1 capital letter 0 otherwise
 **/
int _isupper(int c)
{
if (c >= 65 && c <= 90)
{
return (1);
}
return (0);
}
